//import 'dart:core';

class CustomerS {
  CustomerS(
      this.id, this.name, this.create_date, this.closed_date, this.closed);
  int id;
  String name;
  DateTime create_date;
  DateTime closed_date;
  int closed;
}
